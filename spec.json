{
  "openapi": "3.0.2",
  "info": {
    "title": "Dependency Checker",
    "version": "0.2.0"
  },
  "servers": [
    {
      "url": "https://dependency-checker.test/api",
      "description": "local test server"
    }
  ],
  "paths": {
    "/repository": {
      "get": {
        "summary": "Returns a list of all repositories.",
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/Repository"
                  }
                }
              }
            }
          }
        }
      },
      "post": {
        "summary": "Creates a new repository.",
        "description": "If a repository with the given url already exists, a reference to it is returned.",
        "responses": {
          "200": {
            "description": "Repository was successfully created or already existed.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Repository"
                }
              }
            },
            "links": {
              "GetRepository": {
                "operationId": "getRepository",
                "parameters": {
                  "repositoryId": "$response.body#/id"
                }
              }
            }
          }
        }
      }
    },
    "/repository/{repositoryId}": {
      "get": {
        "summary": "Returns a specific Repository.",
        "operationId": "getRepository",
        "parameters": [
          {
            "name": "repositoryId",
            "in": "path",
            "schema": {
              "type": "integer"
            },
            "required": true
          }
        ],
        "responses": {
          "200": {
            "description": "Repository exists and was returned.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Repository"
                }
              }
            }
          },
          "404": {
            "description": "Repository doesn't exist."
          }
        }
      },
      "delete": {
        "summary": "Deletes the repository",
        "operationId": "deleteRepository",
        "parameters": [
          {
            "name": "repositoryId",
            "in": "path",
            "schema": {
              "type": "integer"
            },
            "required": true
          }
        ],
        "responses": {
          "200": {
            "description": "Repository existed and was deleted."
          },
          "404": {
            "description": "Repository doesn't exist."
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "Repository": {
        "properties": {
          "url": {
            "type": "string",
            "example": "https://github.com/unwanted-consulting/DevelopmentBestPractices"
          }
        },
        "required": [
          "url"
        ]
      }
    }
  }
}
